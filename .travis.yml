language: php

sudo: false

cache:
  directories:
    - $HOME/.composer/cache

php:
  - 7.1

install:
    - composer install

before_script:
    - composer run build:expression-parser
    - composer dumpautoload

script:
    - vendor/bin/phpunit
