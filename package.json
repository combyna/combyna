{
  "version": "0.0.1",
  "name": "combyna",
  "browserify": {
    "transform": [
      "phpify"
    ]
  },
  "phpify": {
    "phpToJS": {
      "include": [
        "php/dist/**/*.php",
        "php/src/**/*.php",
        "vendor/autoload.php",
        "vendor/composer/**/*.php",
        "vendor/symfony/event-dispatcher/(!Tests)**/*.php"
      ],
      "sync": true
    }
  },
  "dependencies": {
    "dotphp": "~2"
  },
  "devDependencies": {
    "browserify": "^13.1.0",
    "phpify": "^3.0.1"
  },
  "scripts": {
    "build:release:client": "mkdir -p dist && cd dist && browserify ../js/client --outfile ./client.js",
    "build:debug:client": "mkdir -p dist && cd dist && browserify --debug ../js/client --outfile ./client.js",
    "server:node": "dotphp php/server.php",
    "server:zend": "php php/server.php"
  }
}
